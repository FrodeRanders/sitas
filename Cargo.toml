[package]
name = "sitas"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
description = "A small Rust experiment in shard-local ownership and typed message passing."
readme = "README.md"
license = "Apache-2.0"

[dependencies]
