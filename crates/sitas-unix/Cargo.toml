[package]
name = "sitas-unix"
version = "0.1.0"
edition = "2024"
description = "sitas Unix backend (std-based, epoll/kqueue/poll/io_uring)"
license = "MIT"
repository = "https://github.com/FrodeRanders/sitas"

[dependencies]
sitas-core = { path = "../sitas-core" }
libc = "0.2"
