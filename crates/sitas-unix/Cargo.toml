[package]
name = "sitas-unix"
version = "0.2.0"
edition = "2024"
description = "sitas Unix backend: a std ShardRuntime over the sitas-core no_std lane"
license = "MIT"
repository = "https://github.com/FrodeRanders/sitas"

[dependencies]
sitas-core = { path = "../sitas-core", features = ["std"] }
